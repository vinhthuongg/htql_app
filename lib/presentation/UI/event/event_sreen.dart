import 'package:flutter/material.dart';

class EventSreen extends StatelessWidget {
  const EventSreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Event Screen')),
    );
  }
}